---
description: Commit changed files in logical groups with detailed messages and push
---

Based on your knowledge of the project, commit all currently changed files in a series of logically connected groupings, each with a detailed commit message, then push.

- Take your time to group related changes together rather than dumping everything into one commit.
- Do NOT edit the code — this is a commit-and-push task only.
- Do NOT commit obviously ephemeral files (build output, local scratch, secrets, editor cruft). Stage specific files rather than `git add .`.
- Push to a new branch rather than directly to `main`/`master` unless I tell you otherwise, and use `git push -u` to set up tracking.
